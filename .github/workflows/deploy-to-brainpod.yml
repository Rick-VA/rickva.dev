name: Deploy to Brainpod

on:
  push:
    branches: ["master"]
    tags: ["v*"]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: brainpod-areas-go
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5

      - name: Login to Brainpod registry
        uses: docker/login-action@v3
        with:
          registry: registry.test.brainpod.io
          username: ${{ github.actor }}
          password: ${{ secrets.BRAINPOD_API_KEY }}

      - name: Build and push
        uses: brainhivenl/steiger-action@v5
        env:
          BUILD_EVENTS_ENDPOINT: https://api.test.brainpod.io
          BUILD_EVENTS_TOKEN: ${{ secrets.BRAINPOD_POD_NAME }}:${{ secrets.BRAINPOD_API_KEY }}
          BUILD_EVENTS_TARGET: urn:brainpod:pod:${{ secrets.BRAINPOD_POD_ID }}
        with:
          cmd: build
          builders: railpack
          args: --repo=registry.test.brainpod.io/areas-go
